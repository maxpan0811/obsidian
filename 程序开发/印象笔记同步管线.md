## 2026-06-23 — Plan: 印象笔记同步管线重构

**Skill**: writing-plans
**项目**: 印象笔记同步管线
**状态**: 计划中

### 实施计划

**Goal**: 将 `export_favorite_md.py` 的 JSON 状态 + 单步导出重构为 SQLite 驱动、四步管线、两阶段写入的同步系统，保证 Evernote Favorites 与 Obsidian .md 文件数量 100% 一致。

**Architecture**: 单一入口 `sync_favorites.py` 编排四步管线：List → Reconcile → Export（两阶段写入）→ Verify。SQLite 替代 processed.json。

---

### Task 1: db.py — SQLite 封装
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/db.py`
- 表：notes / pipeline_state / export_log / export_queue / batch_notes
- CRUD：save_notes, get_all_notes, update_note, batch_replace_batch_notes, set/get_checkpoint, replace/get_export_queue

### Task 2: reconcile.py — 拉取 + 对账
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/reconcile.py`
- Step 1 List：findNotesMetadata 分页拉取 → batch_notes + checkpoint
- Step 2 Reconcile：batch_notes vs notes → export_queue（new / updated）
- orphan 检测（不在 batch 但 DB 有）
- `python3 reconcile.py` 独立运行

### Task 3: export_note.py — 两阶段导出
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/export_note.py`
- Stage A：getNote → 图片 → markdownify → 截断检测 → 写 _pending/{guid}.md
- Stage B：原子 rename → DB 事务 commit
- 非图片附件 → 移入 temp
- ENML 截断检测（不闭合 `</en-note>` → failed）
- 纯文本截断检测（ENML 纯文本 > 50 且 MD 纯文本 < 15 → 报警）

### Task 4: sync_favorites.py — 入口编排器
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/sync_favorites.py`
- `--list`, `--reconcile`, `--export`, `--verify`, `--resume`, `--stats`
- rate limit 重试 + 断点续传

### Task 5: verify.py — 全量验证
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/verify.py`
- GUID 级：文件存在、SHA256、零尺寸、截断、orphan

### Task 6: migrate_from_json.py — 旧数据迁移
- 创建 `/Users/panbo/.claude/skills/印象笔记管理工具/migrate_from_json.py`
- 一次性将 processed.json 导入 SQLite

### Task 7: 更新 SKILL.md
- 添加新管线命令

### Task 8: 14 个 superpower skill + Obsidian 持久化
- 每个 skill 末尾追加持久化规则

### 关键改进
- 两阶段写入：崩溃不丢状态
- 断点续传：checkpoint + --resume
- 截断检测：ENML vs MD 纯文本长度
- 变更感知：updated 时间戳 → 自动重导
- GUID 级 Verify

## 2026-06-24 — Build & Verify

**Skill**: executing-plans
**项目**: 印象笔记同步管线
**状态**: 已实施，全量导出后台运行中

### 已完成文件

| 文件 | 职责 |
|------|------|
| `db.py` | SQLite 封装，5 张表 + CRUD |
| `reconcile.py` | Step 1 List + Step 2 Reconcile |
| `export_note.py` | Step 3 两阶段导出 + 截断检测 |
| `sync_favorites.py` | 入口编排 + 断点续传 |
| `verify.py` | Step 4 全量验证 |
| `migrate_from_json.py` | 旧数据迁移 |

### 迁移统计

- processed.json → SQLite：9691 条
- Favorites 当前：10226 条（+535 新笔记）
- Export queue：10226（9626 updated + 600 new）

### 已知问题

- [`reconcile.py`](reconcile.py:18) — 漏导入 `get_batch_notes`，已修复
- `updated=0` 迁移数据全部标记为需要更新，导致首次全量导出量偏大

### 后台导出

```bash
# 启动
nohup sync_favorites.py --export > /tmp/sync-logs/export.log 2>&1 &

# 查进度
tail -10 /tmp/sync-logs/export.log
```

### Superpowers Skill 更新

14 个 superpower skill 末尾追加了 Obsidian 持久化规则：结构化产出自动 append 到 `程序开发/{项目名}.md`，仅追加不覆盖。

---

## 2026-06-24 15:00 — SKILL.md 经验固化

**Skill**: executing-plans
**项目**: 印象笔记同步管线
**状态**: 后台导出中 (990/9088)

### 新增决策记录

- 重复标题处理策略变更：全部移入 temp，一条不留
- failed 笔记分类：ENML 截断 → 移入 temp；通用错误 → 仅标记
- Fast-path 优化：已导出且 .md 存在的笔记跳过 API 调用
- reconcile 后自动同步 timestamp，下次只跑真正变更的笔记

### 后台导出

```bash
nohup python3 -u sync_favorites.py --export > /tmp/sync-logs/export.log 2>&1 &
```

---

## 2026-06-25 — 首次全量导出进度

**状态**: 已处理 4345/8312 (52%)，后台进程暂停
**后台进程**: PID 93107 (sync_favorites.py --export)，已暂停检查循环

### 关键发现

**ENML 截断假阳性**: `content.endswith('</en-note>')` 对末尾带 `\n` 换行的笔记判假。
修复方向：改为 `content.strip().endswith('</en-note>')`。
影响范围：355 条 failed 笔记可能多数是假阳性，跑完统一修复。

### 已知累计

- 14 个 superpower skill: Obsidian 持久化规则已追加
- 印象笔记管理工具 SKILL.md: 新管线设计决策已记录

---

Architecture: 单一入口 sync_favorites.py 编排四步管线：List, Reconcile, Export, Verify。

Tech Stack: Python 3.14+, sqlite3, markdownify, evernote2 thrift

Task 1: db.py SQLite 封装
Task 2: reconcile.py 拉取 + 对账
Task 3: export_note.py 两阶段导出
Task 4: sync_favorites.py 入口编排器
Task 5: verify.py 全量验证
Task 6: migrate_from_json.py 旧数据迁移
Task 7: 更新 SKILL.md
Task 8: 14 个 superpower skill + Obsidian 持久化

2026-06-24 Build and Verify

已完成: db.py, reconcile.py, export_note.py, sync_favorites.py, verify.py, migrate_from_json.py

迁移统计: processed.json 到 SQLite 9691 条, Favorites 10226 条

2026-06-24 15:00 SKILL.md 经验固化

新增决策:
- 重复标题处理策略变更：全部移入 temp，一条不留
- failed 笔记分类：ENML 截断移到 temp；通用错误仅标记
- Fast-path 优化：已导出且 md 存在的笔记跳过 API 调用

2026-06-25 首次全量导出进度

状态: 已处理 4345/8312 (52%)

关键发现: ENML 截断假阳性，content.endswith 对末尾带换行的笔记判假

## 2026-06-27 17:00 — 首次全量导出完成 + ENML 假阳性修复

**状态**: 首次全量导出完成，ENML 截断假阳性已修复，重导任务中暂停

### 最终对账

| 指标 | 值 |
|------|------|
| .md 文件数 | 10168 |
| DB 已导出 | 9069 |
| 临时移入 temp (含重复标题) | 5 |
| 待重导 (pending) | 617 |
| 孤儿 (DB 在但 Favorites 不在) | 714 |

### 关键修复

**ENML 截断假阳性** — `export_note.py:91` 的判断从 `content.endswith('</en-note>')` 改为 `content.strip().endswith('</en-note>')`。因为大量 Evernote 笔记内容末尾带 `\n` 换行符，原条件误判为截断。修复后 641 条 failed 笔记可正常导出。

### 待完成

- 重跑 617 条 pending 笔记 (`sync_favorites.py --export`)
- 跑 `--verify` 确认最终对齐
- 清理队列残留 (`DELETE FROM export_queue`)