name: Sync to Gitee

on:
  push:
    branches: [main]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Force push to Gitee
        run: |
          git remote add gitee https://maxpan0811:${{ secrets.GITEE_TOKEN }}@gitee.com/maxpan0811/obsidian.git
          git push -f gitee main
